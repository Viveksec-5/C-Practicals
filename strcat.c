#include <stdio.h>
#include <string.h>
// NOTE: apne aap se gap nhi ayega khud se gap daalna hoga.

int main()
{
    char firstr[] = "Vivek ";
    char secstr[] = "Maurya";
    strcat(firstr, secstr);
    puts(firstr);
    return 0;
}